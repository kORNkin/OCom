#include<bits/stdc++.h>
using namespace std;

struct Edge{
    int u, w;
    Edge() { }
    Edge(int u, int w): u(u), w(w) { } 
};

vector<Edge> adj[];
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int V, E; cin >> V >> E;
    for(int i = 1; i <= E; i++){
        int u, v, w; cin >> u >> v >> w;
    
    }

    return 0;
}