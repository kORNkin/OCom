#include<bits/stdc++.h>
using namespace std;
#define ll long long

ll arr[200005], dis[200005];

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    ll n,c;
    cin >> n >> c;
    for(int i = 1; i <= n; i++) cin >> arr[i], dis[i] = LLONG_MAX;

    return 0;
}

