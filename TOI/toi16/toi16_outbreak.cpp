#include<bits/stdc++.h>
using namespace std;

struct Person{
    int x, y, h;
    bool v;
};

int n, t, f, s, q;
vector<Person> v;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> t >> f >> s >> q;

    return 0;
}